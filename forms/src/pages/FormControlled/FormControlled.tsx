import { Controlled } from '../../containers';

import styles from './FormControlled.module.css';

export const FormControlled = () => {
  return (
    <section className={styles.form_controlled}>
      <Controlled></Controlled>
    </section>
  );
};
