import { Uncontrolled } from '../../containers';

import styles from './FormUncontrolled.module.css';

export const FormUncontrolled = () => {
  return (
    <section className={styles.form_uncontrolled}>
      <Uncontrolled></Uncontrolled>
    </section>
  );
};
